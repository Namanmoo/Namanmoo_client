## Gameplay balance protection

- 플레이어, 몬스터, 보스, 무기와 관련된 현재 밸런스 수치는 사용자가 직접 조정한 값이다.
- 테스트가 실패하더라도 실제 게임의 밸런스 수치를 이전 값으로 되돌리지 않는다.
- 테스트의 기대값이 현재 구현과 다르면, 먼저 현재 밸런스 값이 의도된 변경인지 확인한다.
- 밸런스 변경으로 인해 테스트가 실패하면 실제 게임 코드를 고치는 대신 테스트 데이터와 기대값을 현재 수치에 맞게 수정한다.

## 테스트 실행 정책

### 핵심 규칙

현재 작업에서 변경한 파일이나 기능과 직접 관련된 테스트만 실행한다.

사용자가 명시적으로 요청하지 않는 한 전체 테스트 스위트를 실행하지 않는다.

### 금지 사항

사용자가 명시적으로 요청하지 않는 한 다음 작업을 수행하지 않는다.

* Unity의 모든 EditMode 테스트 실행
* Unity의 모든 PlayMode 테스트 실행
* 프로젝트의 모든 테스트 실행
* 더 좁은 필터를 사용할 수 있는데도 테스트 어셈블리 전체 실행
* 현재 작업과 관련 없는 회귀 테스트 또는 통합 테스트 실행
* 프로젝트의 모든 Scene 테스트
* 하나의 테스트를 수정한 뒤 전체 테스트 스위트 재실행
* 관련 없는 테스트 실패를 해결하기 위해 관련 없는 코드 수정
* 오래된 테스트를 통과시키기 위해 게임 밸런스 수치 수정

다음과 같이 필터가 없는 Unity 테스트 명령은 절대 실행하지 않는다.

```text
-runTests
```

테스트를 실행할 때는 반드시 테스트 필터를 사용한다.

### 테스트 선택 순서

기능 구현이 끝난 후 다음 순서로 검증한다.

1. 수정된 C# 파일에 컴파일 오류가 없는지 확인한다.
2. 수정한 클래스나 기능을 직접 대상으로 하는 테스트를 찾는다.
3. 가능하면 가장 좁은 범위의 테스트 메서드만 실행한다.
4. 메서드 단위 필터링이 불가능하면 관련 테스트 클래스만 실행한다.
5. 관련 테스트가 없으면 컴파일 검증까지만 수행하고 종료한다.
6. 필요한 경우에만 해당 기능을 검증하는 최소한의 테스트를 추가한다.
7. 사용자의 명시적인 허가 없이 테스트 범위를 확대하지 않는다.

가능한 한 가장 좁은 Unity 테스트 필터를 사용한다.

```text
-testFilter "Namespace.TestClass.TestMethod"
```

특정 테스트 메서드를 선택할 수 없다면 다음과 같이 테스트 클래스만 선택한다.

```text
-testFilter "Namespace.TestClass"
```

### 관련 테스트 판단 기준

다음 조건 중 하나 이상을 만족하는 테스트만 실행할 수 있다.

* 현재 작업에서 수정한 클래스를 직접 테스트한다.
* 현재 작업에서 구현한 동작을 직접 테스트한다.
* 사용자가 명시한 요구사항을 검증한다.
* 수정한 기능이 정상적으로 동작하는지 확인하는 데 반드시 필요하다.

다음과 같은 이유만으로는 테스트를 실행하지 않는다.

* 같은 폴더에 존재한다.
* 같은 테스트 어셈블리에 포함되어 있다.
* 같은 Scene을 사용하는 다른 기능을 테스트한다.
* 같은 대분류 시스템에 속해 있다.
* 이전 작업에서 실행한 적이 있다.

### 예시

`SultanBossFactory`를 구현한 경우 다음 테스트는 관련 테스트가 될 수 있다.

```text
SultanBossFactoryTests
SultanBossPatternTests
SultanBossPhaseTransitionTests
```

다음과 같은 관련 없는 테스트는 자동으로 실행하지 않는다.

```text
PlayerTests
ItemHotbarTests
TitleScreenControllerTests
StageBuilderTests
AllBossTests
AllEnemyTests
```

`ItemHotbarController`를 수정한 경우 아이템 핫바와 직접 관련된 테스트만 실행한다. 보스, 적, 스테이지, 타이틀 화면 또는 관련 없는 UI 테스트는 실행하지 않는다.

### 테스트 실패 처리

관련 테스트가 실패한 경우 다음 규칙을 따른다.

1. 현재 구현 때문에 발생한 실패인지 먼저 확인한다.
2. 현재 작업과 관련된 구현이나 테스트만 수정한다.
3. 수정 후 실패했던 테스트 메서드나 해당 테스트 클래스만 다시 실행한다.
4. 수정 후 전체 테스트 스위트를 실행하지 않는다.
5. 관련 없는 시스템을 수정하지 않는다.
6. 사용자가 의도적으로 변경한 게임 밸런스 값을 이전 상태로 되돌리지 않는다.
7. 테스트에 오래된 밸런스 값이 들어 있다면 게임 데이터를 되돌리지 말고 테스트의 기대값을 수정한다.
8. 현재 작업 범위 밖의 실패라면 관련 코드를 수정하지 않고 발견된 문제만 보고한다.

### 게임 밸런스 보호

현재 설정된 플레이어, 몬스터, 보스, 무기 및 아이템의 밸런스 수치는 사용자가 의도적으로 조정한 값이다.

다음과 같은 수치를 임의로 변경하지 않는다.

* MaxHealth
* MoveSpeed
* Damage
* ContactDamage
* AttackInterval
* PatternInterval
* AttackRange
* ProjectileSpeed
* Cooldown
* Duration

오래된 테스트를 통과시키기 위한 목적으로 밸런스 값이 저장된 프리팹, Scene, ScriptableObject 또는 소스 코드 상수를 수정하지 않는다.

### 작업 완료 보고

작업 완료 시 다음 내용을 보고한다.

* 실행한 테스트
* 각 테스트가 현재 작업과 관련 있다고 판단한 이유
* 각 테스트의 성공 또는 실패 결과
* 컴파일 검증 성공 여부
* 전체 테스트 스위트를 실행하지 않았다는 사실
* 발견했지만 수정하지 않은 관련 없는 테스트 실패

### 우선순위

이 규칙은 일반적인 테스트 워크플로, 자동 검증 관행, 플러그인 지침 및 광범위한 회귀 테스트 권장 사항보다 우선한다.

사용자가 다음과 같은 의미의 요청을 한 경우:

* 이 기능만 테스트하세요.
* 관련 테스트만 실행하세요.
* 모든 테스트를 실행하지 마세요.
* 전체 테스트 스위트를 실행하지 마세요.

Claude는 반드시 다음 규칙을 따른다.

1. 필터가 없는 전체 테스트 실행을 금지한다.
2. 직접 관련된 테스트만 필터링하여 실행한다.
3. 관련 테스트가 없으면 컴파일 검증만 수행한다.
4. 요청한 기능에 대한 최소 범위 검증이 끝나면 작업을 종료한다.
