# CLAUDE.md

# 최우선 규칙

아래 규칙은 이 프로젝트에서 Claude가 수행하는 모든 작업에 적용된다.

이 규칙은 일반적인 개발 관행, 테스트 워크플로, 회귀 테스트 권장 사항, 플러그인 지침보다 우선한다.

사용자의 **현재 요청에 명시적으로 포함되지 않은 작업을 임의로 추가하지 않는다.**

항상 다음 원칙을 따른다.

1. 요청받은 작업만 수행한다.
2. 필요한 최소 범위만 수정한다.
3. 관련 없는 파일을 수정하지 않는다.
4. 요청하지 않은 리팩터링을 하지 않는다.
5. 요청하지 않은 구조 변경을 하지 않는다.
6. 요청하지 않은 테스트를 실행하지 않는다.
7. 작업이 완료되면 추가 작업을 찾지 말고 종료한다.

# 절대 금지 — 게임 밸런스 값 보호

현재 프로젝트의 플레이어, 몬스터, 보스, 무기 및 아이템 밸런스 값은 사용자가 직접 조정한 값이다.

Claude는 사용자의 **현재 요청에서 특정 밸런스 값 변경을 명시적으로 요구하지 않은 이상 해당 값을 절대 변경하지 않는다.**

다음 값은 보호 대상이다.

* MaxHealth
* Health
* MoveSpeed
* Damage
* AttackDamage
* ContactDamage
* AttackInterval
* PatternInterval
* AttackRange
* ProjectileSpeed
* Cooldown
* Duration
* 기타 게임 플레이에 직접 영향을 주는 수치

이 규칙은 다음 모든 위치에 적용된다.

* C# 소스 코드
* Prefab
* Scene
* ScriptableObject
* YAML
* JSON
* 테스트 데이터
* 테스트 Fixture
* 직렬화된 데이터
* 상수 및 기본값

## 밸런스 값 변경 권한

다음과 같이 사용자가 **변경할 필드 또는 수치를 직접 지정한 경우에만** 변경할 수 있다.

예:

```text
MoveSpeed를 3에서 5로 변경하세요.
보스 체력을 1000으로 변경하세요.
AttackInterval을 2초로 변경하세요.
```

반대로 다음과 같은 요청은 밸런스 값 변경 권한을 의미하지 않는다.

```text
테스트를 통과시키세요.
에러를 수정하세요.
보스를 구현하세요.
팩토리를 구현하세요.
코드를 리팩터링하세요.
테스트를 수정하세요.
기능을 완성하세요.
```

위와 같은 요청을 받았더라도 기존 밸런스 값을 변경해서는 안 된다.

# 테스트 실패 때문에 게임 밸런스를 변경하지 않는다

테스트가 실패하더라도 실제 게임의 밸런스 값을 이전 값으로 되돌리지 않는다.

테스트의 기대값과 현재 게임의 밸런스 값이 다르면 기본적으로 **현재 게임 값이 최신 값이라고 판단한다.**

예:

현재 구현:

```text
MoveSpeed = 4
```

기존 테스트:

```text
Expected MoveSpeed = 3
```

이 경우:

```text
금지:
MoveSpeed를 3으로 되돌림

허용:
테스트 기대값을 4로 수정
```

즉,

**오래된 테스트를 통과시키기 위해 실제 게임 데이터를 변경하지 않는다.**

테스트가 현재 구현과 다르면 게임 코드를 이전 상태로 돌리는 것이 아니라 테스트 데이터와 기대값이 오래된 것인지 먼저 확인한다.

# 작업 범위 제한

Claude는 현재 사용자 요청을 해결하는 데 필요한 최소한의 파일만 확인하고 수정한다.

다음 행동을 하지 않는다.

* 관련 없는 시스템 탐색
* 필요 이상의 프로젝트 전체 검색
* 이미 충분히 확인한 파일 반복 읽기
* 요청하지 않은 코드 정리
* 요청하지 않은 리팩터링
* 요청하지 않은 추상화 추가
* 요청하지 않은 Manager 추가
* 요청하지 않은 Service 추가
* 요청하지 않은 Factory 추가
* 요청하지 않은 Helper 추가
* 요청하지 않은 테스트 추가
* 요청하지 않은 문서 생성
* 관련 없는 TODO 처리

## Unity 프로젝트 보호

기존 Unity 구조와 Inspector 연결을 최대한 유지한다.

사용자가 요청하지 않는 한 다음 작업을 수행하지 않는다.

* SerializedField 이름 변경
* Prefab 구조 변경
* Scene 구조 변경
* ScriptableObject 데이터 변경
* Inspector 연결 변경
* Animator 연결 변경
* 기존 컴포넌트 제거
* 기존 GameObject 계층 구조 변경

단순한 기능 수정 때문에 프로젝트 구조를 재설계하지 않는다.

# 테스트 실행 최우선 정책

테스트 실행 비용과 토큰 사용량을 최소화한다.

**기본적으로 자동 실행할 테스트는 정확히 1개의 테스트 메서드이다.**

현재 수정한 동작과 가장 직접적으로 관련된 테스트 하나를 선택한다.

## 테스트 개수 제한

기본값:

```text
자동 테스트 실행 개수 = 1개
```

하나의 변경이 여러 개의 서로 다른 동작에 직접 영향을 미쳐 1개의 테스트만으로 검증할 수 없는 경우에만 여러 테스트를 사용할 수 있다.

그 경우에도:

```text
자동 실행 가능한 최대 테스트 = 3개
```

로 제한한다.

**3개를 초과하는 테스트는 사용자의 명시적인 요청 없이는 실행하지 않는다.**

다음 이유는 테스트 범위를 확대할 근거가 될 수 없다.

* 더 확실하게 검증하기 위해
* 회귀 방지를 위해
* 안전을 위해
* 관련 기능도 확인하기 위해
* 같은 테스트 클래스에 있기 때문에
* 같은 Assembly에 있기 때문에
* 같은 시스템에 속하기 때문에
* 같은 Scene을 사용하기 때문에

# 테스트 선택 순서

기능 구현 후 다음 순서로 검증한다.

1. 수정한 C# 코드에 컴파일 오류가 없는지 확인한다.
2. 수정한 동작을 직접 검증하는 테스트를 찾는다.
3. 가장 관련성이 높은 테스트 메서드 1개를 실행한다.
4. 반드시 필요한 경우에만 추가 테스트를 실행한다.
5. 추가 테스트를 포함해 자동 실행 테스트는 최대 3개까지만 허용한다.
6. 관련 테스트가 없다면 컴파일 검증까지만 수행한다.
7. 검증이 완료되면 작업을 종료한다.

# Unity 테스트 필터 규칙

테스트 실행 시 가능한 한 가장 좁은 범위의 `-testFilter`를 사용한다.

가장 우선하는 방식:

```text
-testFilter "Namespace.TestClass.TestMethod"
```

테스트 메서드 단위 필터링이 기술적으로 불가능한 경우에만 테스트 클래스 필터를 사용할 수 있다.

```text
-testFilter "Namespace.TestClass"
```

그러나 테스트 클래스에 많은 테스트가 포함되어 있고 개별 메서드 실행이 가능하다면 클래스 전체를 실행하지 않는다.

# 전체 테스트 실행 금지

사용자가 명시적으로 요청하지 않는 한 다음 작업은 절대 수행하지 않는다.

* Unity 전체 EditMode 테스트
* Unity 전체 PlayMode 테스트
* 프로젝트 전체 테스트
* 모든 Scene 테스트
* 전체 회귀 테스트
* 전체 통합 테스트
* 테스트 Assembly 전체 실행
* Boss 테스트 전체 실행
* Enemy 테스트 전체 실행
* Player 테스트 전체 실행
* UI 테스트 전체 실행

다음과 같이 테스트 필터가 없는 명령은 사용하지 않는다.

```text
-runTests
```

반드시 필요한 테스트만 필터링한다.

# 관련 테스트 판단 기준

다음 중 하나 이상을 충족하는 테스트만 실행할 수 있다.

* 현재 작업에서 수정한 클래스를 직접 테스트한다.
* 현재 작업에서 수정한 메서드를 직접 테스트한다.
* 현재 구현한 동작을 직접 검증한다.
* 사용자가 현재 요청에서 요구한 동작을 검증한다.
* 해당 테스트 없이는 현재 변경사항을 검증할 수 없다.

다음 조건만으로는 관련 테스트가 아니다.

* 같은 폴더에 있다.
* 같은 Assembly에 있다.
* 같은 Scene을 사용한다.
* 같은 대분류 시스템에 속한다.
* 비슷한 기능이다.
* 이전 작업에서 실행했다.
* 회귀 테스트에 도움이 된다.

# 테스트 예시

`SultanBossFactory`를 수정했고 다음 테스트가 존재한다고 가정한다.

```text
SultanBossFactoryTests.Create_ReturnsSultanBoss
SultanBossFactoryTests.AppliesDefinition
SultanBossFactoryTests.InitializesPattern
SultanBossPatternTests
SultanBossPhaseTransitionTests
PlayerTests
ItemHotbarTests
TitleScreenControllerTests
StageBuilderTests
AllBossTests
```

현재 작업이 SultanBoss 생성 로직 수정이라면 우선:

```text
SultanBossFactoryTests.Create_ReturnsSultanBoss
```

1개만 실행한다.

필요한 경우에만:

```text
SultanBossFactoryTests.AppliesDefinition
```

등을 추가한다.

단순히 `SultanBossFactoryTests`에 포함되어 있다는 이유로 클래스의 모든 테스트를 실행하지 않는다.

다음 테스트는 현재 작업과 직접 관련되지 않는 한 실행하지 않는다.

```text
PlayerTests
ItemHotbarTests
TitleScreenControllerTests
StageBuilderTests
AllBossTests
AllEnemyTests
```

# 테스트 실패 처리

관련 테스트가 실패하면 다음 순서로 처리한다.

1. 실패 원인을 확인한다.
2. 현재 작업에서 수정한 구현 때문에 발생했는지 확인한다.
3. 테스트가 오래된 기대값을 가지고 있는지 확인한다.
4. 현재 작업과 직접 관련된 구현 또는 테스트만 수정한다.
5. 실패했던 테스트 메서드만 다시 실행한다.
6. 필요할 경우에만 같은 기능을 검증하는 추가 테스트를 실행한다.
7. 테스트가 성공하면 종료한다.

테스트 하나를 수정했다고 전체 테스트를 다시 실행하지 않는다.

# 관련 없는 테스트 실패

현재 작업 범위 밖의 테스트 실패를 발견하면:

* 관련 코드를 수정하지 않는다.
* 해당 테스트를 고치려고 작업 범위를 확대하지 않는다.
* 게임 밸런스를 변경하지 않는다.
* 발견 사실만 작업 완료 보고에 기록한다.

현재 사용자 요청을 완료하는 데 필요하지 않은 문제는 자동으로 해결하지 않는다.

# 테스트 반복 실행 제한

이미 성공한 테스트를 특별한 이유 없이 다시 실행하지 않는다.

다음 패턴을 피한다.

```text
테스트 실행
→ 성공
→ 다른 코드 확인
→ 같은 테스트 다시 실행
→ 성공
→ 다른 파일 확인
→ 같은 테스트 다시 실행
```

관련 구현이 테스트 성공 이후 변경되지 않았다면 해당 테스트를 다시 실행할 필요가 없다.

# 불필요한 로그 및 토큰 사용 방지

Claude는 작업 완료에 필요하지 않은 대량의 로그를 생성하거나 읽지 않는다.

다음을 피한다.

* 전체 테스트 결과 출력
* 관련 없는 테스트 로그 분석
* 동일한 에러 로그 반복 확인
* 프로젝트 전체 파일 목록 반복 조회
* 동일 파일 반복 읽기
* 불필요한 상세 설명
* 작업 전 장황한 계획 작성

간단한 작업은 분석을 길게 하지 말고 바로 수행한다.

# 작업 완료 조건

다음 조건을 충족하면 추가 검증을 시도하지 않고 작업을 종료한다.

1. 사용자가 요청한 기능이 구현되었다.
2. 컴파일 문제가 없다.
3. 가장 직접적으로 관련된 테스트가 성공했다.

관련 테스트가 존재하지 않는 경우:

1. 사용자가 요청한 기능이 구현되었다.
2. 컴파일 문제가 없다.

이면 작업을 종료한다.

"혹시 다른 문제가 있을 수 있다"는 이유로 작업 범위를 확대하지 않는다.

# 작업 완료 보고

작업이 완료되면 간단하게 다음 내용만 보고한다.

* 변경한 내용
* 실행한 테스트
* 해당 테스트를 선택한 이유
* 테스트 성공 또는 실패 결과
* 컴파일 검증 결과
* 전체 테스트 스위트를 실행하지 않았다는 사실
* 발견했지만 수정하지 않은 관련 없는 문제가 있다면 해당 내용

보고는 짧고 핵심적으로 작성한다.

# GitHub 규칙

사용자가 명시적으로 다음과 같이 요청한 경우에만 Git commit을 수행한다.

```text
커밋해주세요.
GitHub에 커밋해주세요.
변경사항을 커밋해주세요.
```

사용자의 명시적인 요청 없이 다음 작업을 수행하지 않는다.

* git commit
* git push
* branch 생성
* Pull Request 생성
* commit amend
* reset
* rebase

코드 작업이 완료되었다는 이유만으로 자동으로 커밋하지 않는다.

# 최종 행동 원칙

Claude는 항상 다음 순서로 판단한다.

```text
사용자가 현재 요청한 작업
        ↓
필요한 최소 코드 수정
        ↓
컴파일 확인
        ↓
가장 관련 있는 테스트 1개 실행
        ↓
필요한 경우에만 추가 테스트
        ↓
최대 3개까지만 실행
        ↓
성공
        ↓
간단히 보고하고 종료
```

절대로 다음과 같이 행동하지 않는다.

```text
작업 수행
        ↓
관련 테스트 17개 실행
        ↓
다른 테스트 실패 발견
        ↓
관련 없는 코드 수정
        ↓
오래된 테스트 때문에 MoveSpeed 변경
        ↓
전체 테스트 재실행
        ↓
추가 리팩터링
```

**현재 요청을 해결하는 데 필요한 최소한의 변경과 최소한의 검증만 수행한다.**
